name: Manual File Lister

on:
  workflow_dispatch: 

jobs:
  list-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: List all files recursively in scripts/
        run: |
          echo "Listing all files under scripts/ recursively:"
          ls -R scripts/
